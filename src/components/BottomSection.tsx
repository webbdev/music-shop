import Contact from "./Contact"
import Map from "./Map"

const BottomSection = () => {
	return (
		<section id="contact-map">
			<Contact />
			<Map />
		</section>
  	)
}

export default BottomSection